#/bin/sh

for i in {0..7} 
    do
    wingo-cmd "RenameWorkspace ${i} \"tttr-${i}\""
done

wingo-cmd 'RenameWorkspace 0 "aux-loptop"'
wingo-cmd 'RenameWorkspace 1 "main-laptop"'
wingo-cmd 'RenameWorkspace 2 "ha"'
wingo-cmd 'RenameWorkspace 3 "staging"'
